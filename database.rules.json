{
  "rules": {
    "characters":{
      ".read": "auth.uid != null",
    	".write": false
    },

    "leaderboard":{
      ".read": "auth.uid != null",
    	".write": "auth.uid != null"
    }
  }
}

